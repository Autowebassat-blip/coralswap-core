[package]
name = "coralswap-shared"
version = "0.1.0"
edition = "2021"

[lib]
doctest = false

[dependencies]
redstone = { workspace = true }
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
